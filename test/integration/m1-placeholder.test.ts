import { describe, it } from 'vitest';

// M1.1 placeholder — real integration tests land in M1.6.
describe('integration (M1.1 placeholder)', () => {
  it('placeholder — scaffolding only', () => {
    // intentionally empty
  });
});
