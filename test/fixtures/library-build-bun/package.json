{
  "name": "@dotnet-wasm-bundler/library-build-bun-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "bun bun.build.ts",
    "test": "echo \"No tests in fixture — build-only package\" && exit 0",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "bun-types": "latest",
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "unplugin-dotnet-static-assets": "workspace:*"
  }
}
