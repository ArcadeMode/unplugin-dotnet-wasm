{
  "name": "@dotnet-wasm-bundler/library-app-node-esbuild-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node esbuild.build.mjs",
    "test": "node dist/entry.js",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "unplugin-dotnet-wasm": "workspace:*"
  }
}

