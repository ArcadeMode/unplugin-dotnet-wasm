{
  "name": "@dotnet-wasm-bundler/library-app-node-webpack-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "webpack --config webpack.config.mjs --mode development",
    "build:debug": "webpack --config webpack.config.mjs --mode development",
    "build:release": "webpack --config webpack.config.mjs --mode production",
    "test": "node dist/entry.js",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^6.0.0",
    "ts-loader": "^9.0.0",
    "typescript": "^5.5.0",
    "webpack": "^5.109.2",
    "webpack-cli": "^7.2.2"
  },
  "dependencies": {
    "unplugin-dotnet-wasm": "workspace:*"
  }
}
