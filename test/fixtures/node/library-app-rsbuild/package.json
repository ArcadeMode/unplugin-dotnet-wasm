{
  "name": "@dotnet-wasm-bundler/library-app-node-rsbuild-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "rsbuild build --env-mode development",
    "build:debug": "rsbuild build --env-mode development",
    "build:release": "rsbuild build --env-mode production",
    "test": "node dist/entry.js",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@rsbuild/core": "^2.1.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "unplugin-dotnet-wasm": "workspace:*"
  }
}
