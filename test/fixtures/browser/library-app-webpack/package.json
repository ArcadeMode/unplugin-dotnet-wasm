{
  "name": "@dotnet-wasm-bundler/library-app-browser-webpack-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "webpack --config webpack.config.mjs --mode development",
    "build:debug": "webpack --config webpack.config.mjs --mode development",
    "build:release": "webpack --config webpack.config.mjs --mode production",
    "dev": "webpack serve --config webpack.config.mjs --port 5174 --mode development",
    "dev:release": "webpack serve --config webpack.config.mjs --port 5174 --mode production",
    "preview": "npx serve dist",
    "test": "echo \"No tests in fixture — build-only package\" && exit 0",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "html-webpack-plugin": "^5.0.0",
    "rimraf": "^6.0.0",
    "ts-loader": "^9.0.0",
    "typescript": "^5.5.0",
    "webpack": "^5.0.0",
    "webpack-cli": "^7.0.0",
    "webpack-dev-server": "^5.0.0"
  },
  "dependencies": {
    "unplugin-dotnet-wasm": "workspace:*"
  }
}

